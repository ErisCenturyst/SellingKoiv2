@model IEnumerable<SellingKoi.Models.Farm>

<<<<<<< HEAD
<div class="container mt-5">
    <div class="row mb-4">
        <div class="col-12 text-center">
            <h1 class="display-4 mb-3">Danh sách Trang trại</h1>
            <div class="alert alert-info d-inline-block">
                <i class="fas fa-info-circle me-2"></i>
                Chọn một trang trại để xem thông tin chi tiết!
            </div>
        </div>
    </div>

    <div class="row">
        @foreach (var farm in Model)
        {
            <div class="col-md-4 col-lg-3 mb-4">
                <div class="farm-card card h-100 shadow-sm">
                    <div class="card-img-wrapper">
                        <img src="@farm.AvatarUrl" class="card-img-top" alt="@farm.Name" 
                             onclick="viewFarmDetails('@farm.Id')" style="cursor: pointer;">
                        <div class="card-img-overlay">
                            <span class="badge bg-primary size-badge">
                                <i class="fas fa-ruler-combined me-1"></i>
                                @farm.Size m²
                            </span>
                        </div>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">@farm.Name</h5>
                        <p class="card-text">
                            <i class="fas fa-user me-2 text-muted"></i>
                            Chủ trại: @farm.Owner
                        </p>
                        <button class="btn btn-outline-primary w-100" onclick="viewFarmDetails('@farm.Id')">
                            <i class="fas fa-info-circle me-2"></i>Xem chi tiết
                        </button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .farm-card {
        transition: all 0.3s ease;
        border: none;
        border-radius: 15px;
        overflow: hidden;
    }

    .farm-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(0,0,0,0.1) !important;
    }

    .card-img-wrapper {
        position: relative;
        height: 200px;
        overflow: hidden;
    }

    .card-img-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: transform 0.3s ease;
    }

    .farm-card:hover .card-img-wrapper img {
        transform: scale(1.05);
    }

    .card-img-overlay {
        position: absolute;
        top: 10px;
        right: 10px;
        background: none;
    }

    .size-badge {
        font-size: 0.8rem;
        padding: 0.5em 1em;
        background-color: rgba(13, 110, 253, 0.9) !important;
    }

    .card-body {
        padding: 1.5rem;
    }

    .card-title {
        font-size: 1.25rem;
        font-weight: 600;
        margin-bottom: 0.75rem;
        color: #2c3e50;
    }

    .card-text {
        color: #6c757d;
        font-size: 0.9rem;
        margin-bottom: 1rem;
    }

    .btn-outline-primary {
        border-width: 2px;
        font-weight: 500;
        padding: 0.5rem 1rem;
        transition: all 0.2s ease;
    }

    .btn-outline-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .alert-info {
        background-color: #e3f2fd;
        border-color: #90caf9;
        color: #0d47a1;
        border-radius: 10px;
        padding: 1rem 2rem;
        margin-bottom: 2rem;
    }

    .display-4 {
        font-weight: 600;
        color: #2c3e50;
        margin-bottom: 1.5rem;
    }

    /* Responsive adjustments */
    @@media (max-width: 768px) {
        .col-md-4 {
            padding: 0 10px;
        }

        .farm-card {
            margin-bottom: 20px;
        }

        .display-4 {
            font-size: 2rem;
        }
    }

    /* Animation for cards */
    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .farm-card {
        animation: fadeIn 0.5s ease-out;
    }
</style>

<script>
    function viewFarmDetails(farmId) {
        window.location.href = `/Farm/DetailsShoppingFarm/${farmId}`;
    }

    // Optional: Add smooth scroll to top when page loads
    window.onbeforeunload = function () {
        window.scrollTo(0, 0);
    }
</script>
=======
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách Trang trại</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .farm-container {
            display: flex;
            flex-wrap: wrap;
        }

        .farm-item {
            border: 1px solid #ccc;
            border-radius: 5px;
            margin: 10px;
            padding: 10px;
            width: 250px;
            text-align: center;
        }

            .farm-item img {
                max-width: 100%;
                height: auto;
                border-radius: 5px;
            }

        .view-details {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
            margin-top: 10px;
        }

            .view-details:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>

    <div class="container-fluid px-0">
        <h1 class="text-center mb-4">Danh sách Trang trại</h1>
        <span>Chọn một trang trại để xem thông tin chi tiết!</span>
        <div class="row farm-container mx-0" id="farmContainer">
            @foreach (var farm in Model)
            {
                <div class="col-md-3 col-sm-6 mb-4">
                    <div class="farm-item">
                        <img src="@farm.AvatarUrl" class="img-fluid" alt="@farm.Name" onclick="viewFarmDetails('@farm.Id')">
                        <h5 class="mt-3">@farm.Name</h5>
                        <p class="mb-1">Chủ trại: @farm.Owner</p>
                        <p class="mb-3">Diện tích: @farm.Size m²</p>
                        <button class="btn btn-primary btn-block view-details" onclick="viewFarmDetails('@farm.Id')">Xem chi tiết</button>
                    </div>
                </div>
            }
        </div>
    </div>

    <script>
        function viewFarmDetails(farmId) {
            window.location.href = `/Farm/DetailsShoppingFarm/${farmId}`;
        }
    </script>

</body>
</html>
>>>>>>> 85c932f9196067835fd31f943dac733028fd05c6
