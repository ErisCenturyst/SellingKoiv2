<<<<<<< HEAD
﻿@using System.Linq
@model IEnumerable<SellingKoi.Models.Route>

<div class="container mt-5">
    <div class="row mb-5">
        <div class="col-12">
            <h1 class="text-center mb-4">Danh sách Lộ trình</h1>
            <div class="alert alert-info">
                <h4 class="alert-heading mb-3">Gợi ý trang trại phù hợp</h4>
                <div class="recommended-farms">
                    @if (ViewBag.RecommendedFarms != null && ViewBag.RecommendedFarms.Count > 0)
                    {
                        <div class="row">
                            @foreach (var farm in ViewBag.RecommendedFarms)
                            {
                                <div class="col-md-4 mb-3">
                                    <div class="farm-recommendation d-flex align-items-center p-2 bg-white rounded shadow-sm">
                                        <img src="@farm.AvatarUrl" alt="@farm.Name" class="farm-avatar me-3">
                                        <span class="farm-name">@farm.Name</span>
                                    </div>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <p class="mb-0">Không có trang trại nào được gợi ý.</p>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        @foreach (var route in Model)
        {
            <div class="col-md-6 mb-4">
                <div class="route-card card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title text-primary mb-3">@route.Name</h5>
                        <div class="price-tag mb-3">
                            <span class="h4 text-success">@route.Price.ToString("N0") VNĐ</span>
                        </div>
                        
                        <div class="farms-section">
                            <h6 class="text-muted mb-3">Trang trại đi qua:</h6>
                            <div class="farms-list">
                                @if (route.Farms != null && route.Farms.Any())
                                {
                                    foreach (var farm in route.Farms)
                                    {
                                        <div class="farm-item-small d-flex align-items-center mb-2">
                                            <img src="@farm.AvatarUrl" alt="@farm.Name" class="farm-thumbnail me-2">
                                            <span>@farm.Name</span>
                                        </div>
                                    }
                                }
                                else
                                {
                                    <p class="text-muted">Không có trang trại nào.</p>
                                }
                            </div>
                        </div>
                    </div>
                    <div class="card-footer bg-transparent border-top-0">
                        <button class="btn btn-primary w-100" onclick="addToCart('@route.Id', '@route.Name', '@route.Price')">
                            <i class="fas fa-cart-plus me-2"></i>Thêm vào giỏ hàng
                        </button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    /* Base styles */
    .route-card {
        transition: transform 0.2s;
        border: none;
        border-radius: 15px;
    }

    @@media (max-width: 768px) {
        .route-card {
            margin-bottom: 20px;
        }
    }

    @@keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    .farm-avatar {
        width: 50px;
        height: 50px;
        border-radius: 50%;
        object-fit: cover;
    }

    .farm-thumbnail {
        width: 40px;
        height: 40px;
        border-radius: 8px;
        object-fit: cover;
    }

    .recommended-farms {
        padding: 15px;
        border-radius: 10px;
    }

    .farm-recommendation {
        transition: transform 0.2s;
    }

    .farm-recommendation:hover {
        transform: translateY(-2px);
    }

    .price-tag {
        padding: 8px 15px;
        background-color: #f8f9fa;
        border-radius: 10px;
        display: inline-block;
    }

    .farms-section {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 10px;
        margin-bottom: 15px;
    }

    .btn-primary {
        border-radius: 8px;
        padding: 10px 20px;
        font-weight: 500;
    }

    .alert-info {
        background-color: #e3f2fd;
        border-color: #90caf9;
        color: #0d47a1;
    }
</style>

@section Scripts {
=======
﻿@model IEnumerable<SellingKoi.Models.Route>

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh sách Lộ trình</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }

        .route-container {
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            padding: 10px;
        }

        .add-to-cart {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
        }

        .route-title {
            font-size: 20px;
            font-weight: bold;
        }

        .farm-list {
            margin-top: 10px;
            padding-left: 20px;
        }

        .farm-item {
            margin: 5px 0;
            display: flex;
            align-items: center;
        }

            .farm-item img {
                width: 50px;
                height: 50px;
                object-fit: cover;
                margin-right: 10px;
            }
    </style>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>

    <h1>Danh sách Lộ trình</h1>

    <!-- Hiển thị thông báo và danh sách trang trại -->
    <div>
        <h2>Lộ trình mà bạn chọn nên có các trang trại sau:</h2>
        <div class="farm-list">
            @if (ViewBag.RecommendedFarms != null)
            {
                foreach (var farm in ViewBag.RecommendedFarms)
                {
                    <div class="farm-item">
                        <img src="@farm.AvatarUrl" alt="Hình ảnh trang trại" />
                        @farm.Name
                    </div>
                }
            }
            else
            {
                <div class="farm-item">Không có trang trại nào được gợi ý.</div>
            }
        </div>
    </div>

    @foreach (var route in Model)
    {
        <div class="route-container">
            <div class="route-title">@route.Name</div>
            <div>
                <strong>Giá tiền :</strong> @route.Price.ToString("N0") VNĐ
            </div>
            <div class="farm-list">
                <strong>Trang trại đi qua :</strong>
                @if (route.Farms != null && route.Farms.Any())
                {
                    foreach (var farm in route.Farms)
                    {
                        <div class="farm-item">
                            <img src="@farm.AvatarUrl" alt="Hình ảnh trang trại" />
                            @farm.Name
                        </div>
                    }
                }
                else
                {
                    <div class="farm-item">Không có trang trại nào.</div>
                }
            </div>
            <!-- Thêm nút "Add to Cart" -->
            <button class="add-to-cart" onclick="addToCart('@route.Id', '@route.Name', '@route.Price')">Add to Cart</button>
        </div>
    }
>>>>>>> 85c932f9196067835fd31f943dac733028fd05c6
    <script>
        function addToCart(routeId, routeName, routePrice) {
            // Kiểm tra xem giỏ hàng đã có route nào chưa
            $.ajax({
<<<<<<< HEAD
                url: '/Cart/GetCartItems',
=======
                url: '/Cart/GetCartItems', // Đường dẫn để lấy các mục trong giỏ hàng
>>>>>>> 85c932f9196067835fd31f943dac733028fd05c6
                type: 'GET',
                success: function (response) {
                    if (response.length > 0) {
                        alert('Chỉ cho phép một route trong giỏ hàng. Vui lòng xóa route hiện tại trước khi thêm route mới.');
                    } else {
                        // Nếu giỏ hàng trống, thêm route mới
                        $.ajax({
                            url: '/Cart/AddToCart',
                            type: 'POST',
                            data: JSON.stringify({ Id: routeId, Name: routeName, Price: routePrice }),
                            contentType: 'application/json',
                            success: function (response) {
                                if (response.success) {
                                    alert('Đã thêm vào giỏ hàng!');
<<<<<<< HEAD
                                    // Update cart counter immediately
                                    fetch('/Cart/GetCartCount')
                                        .then(response => response.json())
                                        .then(count => {
                                            const counter = document.getElementById('cartCounter');
                                            if (counter) {
                                                counter.textContent = count;
                                                counter.style.display = count > 0 ? 'inline' : 'none';
                                            }
                                        });
                                } else {
                                    alert('Đã thêm vào giỏ hàng.');
                                    // Update cart counter immediately
                                    fetch('/Cart/GetCartCount')
                                        .then(response => response.json())
                                        .then(count => {
                                            const counter = document.getElementById('cartCounter');
                                            if (counter) {
                                                counter.textContent = count;
                                                counter.style.display = count > 0 ? 'inline' : 'none';
                                            }
                                        });
=======
                                } else {
                                    alert('Đã thêm vào giỏ hàng.');
>>>>>>> 85c932f9196067835fd31f943dac733028fd05c6
                                }
                            },
                            error: function () {
                                alert('Có lỗi xảy ra khi thêm vào giỏ hàng!');
                            }
                        });
                    }
                },
                error: function () {
                    alert('Có lỗi xảy ra khi kiểm tra giỏ hàng!');
                }
            });
        }
    </script>
<<<<<<< HEAD
}
=======
</body>
</html>
>>>>>>> 85c932f9196067835fd31f943dac733028fd05c6
